# Outils / IDE
.capybavibe/
.claude/
.DS_Store

# Journaux d'exécution locaux
refresh.log
*.log

# Dépendances éventuelles
node_modules/

# Exports iCal manuels (perso, non publiés) — on garde juste le mode d'emploi
ics-est-republicain/*.ics

# Import manuel Facebook : page sauvegardée (~60 Mo) — artefact local, non versionné
ics-facebook/

# Build du site statique publié sur Netlify (régénéré par deploy-site.sh)
dist/

# Build du site de staging (régénéré par deploy-staging.sh)
dist-staging/

# Local Netlify folder
.netlify

# Token API Cloudflare (deploy non-interactif du cron) — SECRET, jamais commité
.cloudflare-token
